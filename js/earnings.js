///*Create a script element*/
//var imported =  document.createElement('script');
///*Sets the file that needs to be imported*/
//imported.src = 'main_.js';
///*Appends the script to the head of the script*/
//document.head.appendChild(imported);

window.onload = function () {
    $("h").addEventListener("click",showDiv,false);
    $("c").addEventListener("click",showDiv,false);
    $("f").addEventListener("click",showDiv,false);
};
